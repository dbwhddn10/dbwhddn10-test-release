{
  "name": "dbwhddn10-test-release",
  "version": "0.1.11+build.2",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "release-it": "release-it",
    "semantic-release": "semantic-release",
    "aaaa": "ABCD=$(git symbolic-ref HEAD) && echo ABCD",
    "dev": "COMMIT_SHA=$(git rev-parse HEAD) COMMIT_REF=$(git symbolic-ref HEAD) next dev",
    "version:check-branch": "$branch_name=$(git symbolic-ref -q HEAD)",
    "abcd":"if [ \"aaa\" = \"aaa\" ]; then echo 'abcd' ; fi",
    "publish": "npm publish --access public --tag $(git symbolic-ref --short HEAD)"
  },
  "dependencies": {
    "npm-publish": "^0.4.7",
    "release-it": "^18.1.1",
    "semver": "^7.6.3"
  },
  "devDependencies": {
    "@semantic-release/exec": "^6.0.3",
    "semantic-release": "^24.2.1"
  }
}
